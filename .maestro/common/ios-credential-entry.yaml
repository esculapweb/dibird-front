# iOS's entry point into the shared credential entry — kept as its own file so
# login.yaml and the guest funnel, which reference it by name, did not have to
# change when the step became cross-platform (common/credential-entry.yaml).
#
# appId is inert here — see common/back.yaml.
appId: ${APP_ID}
---
- runFlow: credential-entry.yaml
